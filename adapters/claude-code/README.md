# Claude Code adapter guidance

Use project instructions and bounded agent sessions while retaining a single coordinator of record. Declare which interruption, resume, permission, and structured-result semantics are actually available in the selected execution mode. If a session cannot emit a trustworthy receipt, use supervised handoff and validate artifacts directly.

Gateway or model-provider configuration is user-local infrastructure. Never place access keys in the project adapter or packet.
